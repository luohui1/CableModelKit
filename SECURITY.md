# 安全边界

本项目不是不可信 Python 或 CAD 数据的沙箱。
默认不联网、不监听端口、不导入任意用户 STEP、不执行来自 JSON 的 Python/shell。
仅显式白名单会加载第三方 entry point，但获准插件能够执行 Python，其权限等同运行进程。

生产宿主必须限制插件来源，并使用独立 worker、文件目录隔离、时间／内存预算及可终止的进程。
现有参数大小、尺寸、实体数、显示三角数限制只用于原型输入约束，不替代操作系统级隔离。

输出目录由宿主提供，必须是新目录。失败会清理暂存数据；已有输出不会被覆盖。
请勿把生成几何的校验报告当成电缆设计、载流量或运行安全的认证。
